# Wget configuration file
# This file must exist because WGETRC is set in .zshrc for XDG compliance.
# Wget fails if WGETRC points to a non-existent file.
#
# Without this file, luarocks (which uses wget for downloads) will fail
# when trying to install packages in the Neovim hererocks environment.

# Sensible defaults for modern usage
timestamping = on
no_parent = on
