# shellcheck shell=bash
# ================================================================
# Windows shell layer
# ================================================================
# The os axis layer a Windows machine selects, deployed by `dotfiles symlinks
# apply` like every other coordinate directory.
#
# It sat outside the coordinate scheme as shell/git-bash/ for as long as no
# machine in this fleet was Windows, reaching the Windows side only as a file a
# WSL box copied across the boundary. That bridge is gone: the machine deploys
# this itself.
# ================================================================

alias open='start'
