# shellcheck shell=bash
# ================================================================
# Windows shell layer
# ================================================================
# The os axis layer a Windows machine selects, deployed by `dotfiles symlinks
# apply` like every other coordinate directory. A Windows box selects it for
# itself, the same way it selects everything else it runs.
# ================================================================

alias open='start'
