# shellcheck shell=bash
# shellcheck disable=SC2154
# SC2154 = Variables referenced but not assigned (from sourced files)

# Docker functions are currently included in core.sh (ubuntu-docker).
# This file exists for future docker-specific functions.
