# shellcheck shell=bash
# ================================================================
# Windows Git Bash shell overrides
# ================================================================
# Copied to Windows ~/.local/shell/ by install/wsl/sync-windows-shell.sh and
# sourced last, so anything here wins over the shared files. Named for
# $PLATFORM, which is how `menu` finds a platform's functions and aliases.
# ================================================================

# macOS 'open' equivalent
alias open='start'
