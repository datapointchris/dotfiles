---
icon: material/tools
---

# Tools

The two front doors into the repo, past what `dotfiles --help` and `task --list-all` already say.

## Pages

- [Symlinks Manager](symlinks.md) — how the three trees deploy into `$HOME`, and what a coordinate
  directory means inside each
- [Task Reference](tasks.md) — what earns a task and what stays a bare command; `task --list-all`
  is the list itself
