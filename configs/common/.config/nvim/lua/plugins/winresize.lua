-- Change the direction of resize based on the window position to be intuitive
-- Regular neovim resizes backwards if you are in the right hand window, this plugin checks and inverts
return {
  'pogyomo/winresize.nvim',
}
