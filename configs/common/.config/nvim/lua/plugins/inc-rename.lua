return {
  'smjonas/inc-rename.nvim',
  -- Neovim's native command-preview drives the live buffer preview; noice's
  -- inc_rename preset supplies the cmdline input, so no input_buffer_type
  -- override is needed here.
  opts = {},
}
