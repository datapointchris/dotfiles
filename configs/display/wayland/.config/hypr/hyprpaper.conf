# hyprpaper configuration
# Wallpapers are set dynamically via theme system using hyprctl

ipc = true
splash = false

# Default fallback wallpaper (empty monitor = all monitors)
# This gets overwritten by theme system on apply
wallpaper {
    monitor =
    path = ~/.local/share/theme/background.png
    fit_mode = cover
}
